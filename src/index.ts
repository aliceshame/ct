import * as index   from './data/index';
import * as library from './data/library';
import * as view from './data/view';
import * as edit from './data/edit';
import * as profile from './data/profile';

const data = {
    index,
    library,
    view,
    edit,
    profile
};

export = data;
